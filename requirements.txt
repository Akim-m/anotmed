# Core runtime (CPU) — enough to run the stub backend, the API, the UI, tests.
# For the real models see the [models] extra in pyproject.toml and the README.
numpy>=1.24
pydicom>=2.4
pydantic>=2.5
fastapi>=0.110
uvicorn>=0.27
Pillow>=10.0
python-multipart>=0.0.9
